{
  "manifest_version": 3,

  "name": "Getting started example",
  "description": "This extension allows to immediately download imslp hosted files without waiting",
  "version": "1.0",
  "content_scripts": [{
    "matches": ["https://imslp.org/*"],
    "js": ["content.js"]
  }],
  "permissions": [
    "activeTab"
  ]
}